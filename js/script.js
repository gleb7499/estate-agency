// Базовый скрипт, изолированный от глобальной области видимости
(() => {
  // Все селекторы берём внутри .mp-root по правилам agent.md, если понадобится
  console.log('Placeholder site loaded');
})();
